/* ==========================================================================
   IHI DESIGN SYSTEM — PRIMARY CORE UI EXPORT ENTRYPOINT
   ========================================================================== */

// --- Button Component & Context Types ---
export { 
  Button, 
  type ButtonProps, 
  type ButtonVariant, 
  type ButtonSize 
} from "./Button";

// --- Card Component, Composables & Layout Types ---
export { 
  Card, 
  CardHeader, 
  CardBody, 
  CardFooter, 
  type CardProps, 
  type CardPadding, 
  type CardRadius, 
  type CardVariant 
} from "./Card";

// --- Input Field Components & Supporting Types ---
export { 
  Input, 
  type InputProps 
} from "./Input";

// --- Table Components, Layout structures & Action States ---
export { 
  Table, 
  TableHeader, 
  TableBody, 
  TableRow, 
  TableHead, 
  TableCell, 
  TableFooter, 
  TableEmpty 
} from "./Table";

// --- Status Badging, Signal Metrics & Backwards Compatibility ---
export { 
  StatusBadge, 
  type StatusBadgeProps, 
  type Status, 
  type SignalStatus 
} from "./StatusBadge";